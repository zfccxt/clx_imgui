#pragma warning(push)
// sscanf return value unused warnings coming from libraries - nothing we can do about that
#pragma warning(disable : 6031)
// Unscoped enum warnings coming from libraries - nothing we can do about that either
#pragma warning(disable : 26812)

#include <backends/imgui_impl_opengl3.cpp>
#include <backends/imgui_impl_glfw.cpp>

#pragma warning(pop)
